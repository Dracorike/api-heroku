version: '3.8'
services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_DB=vote-api
      - POSTGRES_USER=pedro
      - POSTGRES_PASSWORD=136279804
    expose:
      - "5432"
    ports:
      - "5432:5432"